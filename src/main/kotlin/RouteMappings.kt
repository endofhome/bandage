object RouteMappings {
    const val index = "/"
    const val login = "/login"
    const val logout = "logout"
    const val dashboard = "/dashboard"
    const val metadata = "/tracks/{id}"
    const val upload = "/upload"
    const val uploadPreview = "/upload-preview"
    const val playWithPath = "/play/{id}"

    const val api = "/api"
    const val tracks = "/tracks"

    const val play = "/play"
}